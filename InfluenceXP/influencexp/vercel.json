{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "chmod +x ./install-deps.sh && ./install-deps.sh && npm install --legacy-peer-deps",
        "buildCommand": "npm run build"
      }
    }
  ]
} 